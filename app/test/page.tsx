'use client'
import { useSession } from 'next-auth/react'
import React from 'react'

const page = async () => {
    // const { data: session, status, update } = useSession()
    //  update({ user: { isvarified:true} })


  return (
    <div>page</div>
  )
}

export default page